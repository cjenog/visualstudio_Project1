/*
#include <stdio.h>

int	ft_str_is_numeric(char* str)   // str이 숫자이면 1 아니면 0을 리턴
{
	int i;

	i = 0;
	while (str[i] != 0)
	{
		if (!(str[i] >= '0' && str[i] <= '9'))
			return (0);
		i++;
	}
	return (1);
}
*/