/*
int	ft_str_is_uppercase(char* str)  //대문자만 있는지 확인 
{
	char* charptr;

	charptr = str;
	while (*charptr != 0)
	{
		if (*charptr < 'A' || *charptr > 'Z')
		{
			return (0);
		}
		charptr++;
	}
	return (1);
}
*/