/*
#include <stdio.h>

int	ft_str_is_alpha(char* str)   // 알파벳이면 1을리턴 아니면 0을리턴함.
{
	int i;

	i = 0;
	while (str[i] != 0)
	{
		if (str[i] >= 'a' && str[i] <= 'z')
		{
		}
		else if (str[i] >= 'A' && str[i] <= 'Z')
		{
		}
		else
			return (0);
		i++;
	}
	return (1);
}
*/